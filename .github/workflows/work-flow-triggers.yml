name: Exploring Work Flow Triggers

on: 
  schedule:
    - cron: "2 * * * *" # Runs at every minute

jobs:
  Work_Flow_Triggers:
    runs-on: ubuntu-latest 

    steps:
      - name: Cron Job
        run: echo "Executed Using Cron Job Event"
